info:
  name: config
  type: http
  seq: 3

http:
  method: GET
  url: "{{functionapp_host}}/api/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
